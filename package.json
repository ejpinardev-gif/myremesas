{
  "name": "myremesas",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^1.7.7"
  },
  "scripts": {
    "build:css": "tailwindcss -i ./public/styles/input.css -o ./public/styles/output.css",
    "build:js": "node build.js",
    "build": "npm run build:css && npm run build:js"
  },
  "devDependencies": {
    "postcss": "^8",
    "tailwindcss": "^3.4.1"
  }
}
